# eBaySnipe

eBay listing monitor for computer memory deals. Deployed on Render.

## Render Settings
- **Build Command**: `npm install && npm run build`
- **Start Command**: `npm start`
- **Env Vars**: `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, `EBAY_ENV=production`, `PORT=3001`
